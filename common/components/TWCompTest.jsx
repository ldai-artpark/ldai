import React from 'react';
import PropTypes from 'prop-types';

const TWCompTest = props => {
    return (
        <div className={"h-96"}>
            <h1 className={" font-black text-red-500 text-9xl "}>
                Hello world
            </h1>
        </div>
    );
};

TWCompTest.propTypes = {

};

export default TWCompTest;